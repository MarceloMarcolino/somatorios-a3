; React 19 + react-scripts 5 (peer dependency e React ^18): permite instalar
; sem ERESOLVE em qualquer ambiente (local, Vercel, CI). Equivale ao antigo
; "npm install --legacy-peer-deps", agora versionado para o build da Vercel.
legacy-peer-deps=true
